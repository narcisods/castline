services:
  - type: web
    name: castline-backend
    runtime: node
    plan: free
    buildCommand: corepack enable yarn && yarn install --immutable && yarn workspace backend build
    startCommand: corepack enable yarn && yarn workspace backend start
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false
      - key: NODE_ENV
        value: production
